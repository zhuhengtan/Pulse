# Login troubleshooting example

This example exercises the MVP path `Main → Fork(analyze, tests) → Join → Synthesize` with a deterministic Runtime executor. It intentionally uses no credentials or network calls.
