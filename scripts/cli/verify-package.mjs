import { mkdtemp, readFile, rm, writeFile } from 'node:fs/promises'
import { spawnSync } from 'node:child_process'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
const args = process.argv.slice(2); const index = args.indexOf('--archive'); const archive = index >= 0 ? args[index + 1] : undefined; if (!archive) { console.error('Usage: pnpm cli:verify-package -- --archive <path>'); process.exit(2) } const directory = await mkdtemp(join(tmpdir(), 'pulse-package-verify-')); const extracted = spawnSync('tar', ['-xzf', archive, '-C', directory], { stdio: 'inherit' }); if (extracted.status !== 0) process.exit(extracted.status ?? 1); const bin = join(directory, 'pulse/bin/pulse'); const packageManifest = JSON.parse(await readFile(join(directory, 'pulse/app/node_modules/@hunterzhu/pulse-cli/package.json'), 'utf8')); const versionCheck = spawnSync(process.execPath, [bin, '--version'], { encoding: 'utf8' }); if (versionCheck.status !== 0 || versionCheck.stdout.trim() !== packageManifest.version) { console.error(`CLI version mismatch: expected ${packageManifest.version}, received ${versionCheck.stdout.trim()}`); process.exit(versionCheck.status ?? 1) } const helpCheck = spawnSync(process.execPath, [bin, '--help'], { encoding: 'utf8' }); if (helpCheck.status !== 0) { console.error(helpCheck.stderr); process.exit(helpCheck.status ?? 1) } const cwd = join(directory, 'workspace'); await import('node:fs/promises').then(({ mkdir }) => mkdir(cwd)); await writeFile(join(cwd, 'input.txt'), 'package verification\n'); const task = spawnSync(process.execPath, [bin, 'run', 'say hello', '--mock-response', 'package ok', '--cwd', cwd], { env: { ...process.env, PULSE_CONFIG: join(directory, 'config.json'), PULSE_DATA_DIR: join(directory, 'data') }, encoding: 'utf8' }); if (task.status !== 0) { console.error(task.stderr); process.exit(task.status ?? 1) } console.log('package verification passed'); await rm(directory, { recursive: true, force: true })
